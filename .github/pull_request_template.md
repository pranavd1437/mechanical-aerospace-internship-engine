<!--
Thanks for contributing. Adding a company is genuinely a one-line change —
don't overthink this template, most of it won't apply.
-->

## What this changes


## Why


## Checklist

- [ ] `python -m pytest` passes
- [ ] `ruff check src tests tools run.py` passes
- [ ] If a classification rule changed, there's a test with a **real** posting
      title/location that used to be wrong
- [ ] If output format changed, `python tools/verify_accuracy.py` still passes

<!--
Generated files (README.md, docs/, data/jobs.json, data/stats.json …) are
rebuilt hourly by the bot — no need to include them, and leaving them out keeps
the diff readable.
-->
